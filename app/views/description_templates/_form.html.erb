<%= form_for(@description_template) do |f| %>
  <% if @description_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@description_template.errors.count, "error") %> prohibited this description_template from being saved:</h2>

      <ul>
      <% @description_template.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
