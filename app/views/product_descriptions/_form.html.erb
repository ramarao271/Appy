<%= stylesheet_link_tag "bootstrap.min", :media => "all" %>
    <%= stylesheet_link_tag "bootstrap-table", :media => "all" %>
    <%= javascript_include_tag "jquery-1.11.3.min" %>
    <%= javascript_include_tag "bootstrap.min" %>
    <%= javascript_include_tag "bootstrap-table" %>
    <%= javascript_include_tag "application" %>

<%= bootstrap_form_for(@product_description) do |f| %>
  <% if @product_description.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_description.errors.count, "error") %> prohibited this product_description from being saved:</h2>

      <ul>
      <% @product_description.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.text_area :description %>
  </div>
  <input type="hidden" name="product_id" id="product_id" value="<%= params[:product_id] %>" />
  <div class="actions">
    <%= f.submit %>
  </div>
<script>
  CKEDITOR.replace( 'product_description_description' );
</script>
<% end %>
